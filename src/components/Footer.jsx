const Footer = (props) => {
    return (
        <footer>
            <div id="footer-wrapper">
                <img src={props.logo} alt="Logo" width="80" />
            </div>
        </footer>
    )
}

export default Footer